apiVersion: v1
kind: Service
metadata:
  name: songs-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: songs-service
  ports:
    - port: 8081
      targetPort: 8081