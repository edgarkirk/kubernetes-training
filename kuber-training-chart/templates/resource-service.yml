apiVersion: v1
kind: Service
metadata:
  name: resources-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: resources-service
  ports:
    - port: 8080
      targetPort: 8080