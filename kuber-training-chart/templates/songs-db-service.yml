apiVersion: v1
kind: Service
metadata:
  name: songs-db
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: songs-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432